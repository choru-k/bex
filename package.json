{
  "name": "bex",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @bex/core build",
    "dev:core": "pnpm --filter @bex/core dev",
    "dev:raycast": "pnpm --filter @bex/raycast dev",
    "lint:raycast": "pnpm --filter @bex/raycast lint"
  }
}
