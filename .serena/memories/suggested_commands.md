# Commands

## Build
- `pnpm build` — build all packages
- `pnpm build:core` — build core only

## Dev
- `pnpm dev:core` — watch core
- `pnpm dev:raycast` — dev raycast extension
- `pnpm dev:app` — dev tauri app (planned)

## System
- macOS (Darwin)
- Node.js, pnpm, Rust/Cargo (for Tauri)
